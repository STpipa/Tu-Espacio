# CLAUDE.md — Tu Espacio 🌿⚡

## Identidad y contexto
Este repo es la app **Tu Espacio**: una plataforma inmersiva de constelaciones familiares en un entorno 3D multiusuario en tiempo real (estilo Roblox), combinando la calidez de un centro de sanación con tecnología interactiva moderna.

Origen del proyecto: documento de arquitectura `Tu_Espacio_Documento_Completo_v3.pdf` (subido por el usuario el 2026-07-27), que define stack, esquema de datos, monetización y una guía de 5 prompts secuenciales para construir la app paso a paso con Claude Code. Este CLAUDE.md es la versión "viva" de ese documento: se actualiza a medida que el proyecto avanza (el PDF original queda como referencia histórica, no se vuelve a editar).

**Estado al 2026-07-27:** carpeta recién creada, sin código todavía. Punto de partida: correr el Paso A de la guía (ver más abajo).

## Identificador técnico
- Nombre de la app: **Tu Espacio**
- Package/bundle id: `com.tuespacio.app` (temporal — confirmar antes de compilar con EAS, puede requerir un dominio real invertido si se registra en las tiendas)

## Stack tecnológico

| Capa | Función | Tecnologías clave |
|---|---|---|
| Plataforma móvil | Navegación, UI, perfiles y gestión | Expo (React Native) |
| Backend / BD | Auth, base de datos, storage de assets | Supabase (Auth, Postgres, Storage) |
| Entorno 3D | Renderizado de mundo inmersivo y avatares | React Three Fiber / Three.js |
| Tiempo real | Sincronización de posiciones 3D y chat | WebSockets (Socket.io o Colyseus) |
| Comunicación de voz | Audio espacial de baja latencia en sala | WebRTC (LiveKit o Agora) |
| Pagos / compras | Suscripciones y monetización | RevenueCat |

## Características del entorno 3D y avatares
- **Cámara orbitable 360°**: libertad total para observar ángulos y dirección de mirada de los representantes.
- **Controles de Super Admin / Curador**: congelar movimiento de avatares, teletransportar/forzar posición (X, Y, Z), silenciar audio/micrófono y chat de texto, expulsar (kick) y bloquear (ban) de la sesión.
- **Avatares modulares personalizables**: capas (ej. Superman), accesorios (ej. Martillo de Thor), disfraces (Monje, Rana), botón "Al azar" para combinaciones aleatorias. La configuración se guarda como JSON en Supabase (`avatar_config`).

## Esquema de base de datos (Supabase / PostgreSQL)

**`profiles`**
- `id` (UUID, referencia a `auth.users`)
- `email`
- `role` (`super_admin` | `curador` | `cliente`)
- `exento_pago` (boolean, default `false`)
- `avatar_config` (JSONB)

**`salas_3d`**
- `id` (UUID)
- `curador_id`
- `codigo_acceso`
- `estado`
- `created_at`

**`catalogo_avatares`**
- `id`
- `nombre`
- `categoria`
- `model_url` (modelos GLB en Supabase Storage)
- `es_premium`

**`sesiones_uso`**
- `id`
- `cliente_id`
- `fecha_asistencia` (control del límite mensual de clientes gratuitos)

## Modelo de monetización y permisos
- **Modelo híbrido**: suscripción para curadores + límite de 2 asistencias gratis/mes para clientes + tienda de accesorios de avatares.
- **Bypass para curadores elegidos**: antes de pedir pago por RevenueCat, verificar `exento_pago == true` o `role == 'super_admin'`.
- **Permisos del sistema**: micrófono (audio WebRTC), notificaciones push (avisos de citas), cámara/galería (foto de perfil) — todos con mensajes explicativos claros al usuario.

## Onboarding y publicación
- Onboarding de 3 pantallas, solo la primera vez: (1) Sanación y Conexión, (2) Espacios 3D e Innovación, (3) Sesiones Guiadas en Vivo.
- `app.json` con `name: "Tu Espacio"`, `package: "com.tuespacio.app"`, listo para build con EAS (iOS/Android).

## Roadmap de implementación — 5 pasos secuenciales
Seguir este orden. Marcar cada paso como hecho en "✅ Próximos pasos" (abajo) y dejar un registro en "📌 Decisiones clave" al completarlo.

**Paso A — Autenticación y roles**
Añadir auth con Supabase a la app de Expo. Crear tabla `profiles` vinculada a `auth.users` con `role` (`super_admin`/`curador`/`cliente`) y `exento_pago` (boolean, default false). Sin sesión → pantalla de login. Cliente → vista principal. Curador → panel de creación de salas.

**Paso B — Entorno virtual 3D y avatares modulares**
Integrar React Three Fiber: sala 3D interactiva con cámara orbitable 360°. Cargar avatares leyendo `avatar_config` (JSONB) desde Supabase. Controles de movimiento en el plano 3D + botón "Al azar" que combina accesorios desde `catalogo_avatares`.

**Paso C — Sincronización en tiempo real y moderación**
Conectar la sala 3D con WebSockets para sincronizar posición/rotación en tiempo real. Panel de herramientas exclusivo para `role = 'curador'` o `'super_admin'`: mover cualquier avatar, congelar movimiento, silenciar audio/chat, expulsar usuario.

**Paso D — Monetización y bypass de pagos**
Límites de uso + pagos con RevenueCat. Clientes gratuitos: máximo 2 asistencias/mes (tabla `sesiones_uso`). Antes de dejar crear una sala a un curador, verificar en Supabase: `role == 'super_admin'` OR `exento_pago == true` OR suscripción activa en RevenueCat.

**Paso E — Permisos, onboarding y preparación EAS**
Configurar permisos (micrófono, notificaciones push, cámara/galería) con mensajes explicativos. Onboarding inicial de 3 pantallas, estética holística, solo la primera vez. `app.json` listo (`name`, `package`) para publicar con EAS Build.

## Credenciales y entorno
Todavía **no hay credenciales reales** (2026-07-27). Convenciones a seguir:
- Variables de entorno en `.env` (nunca commitear), con prefijo `EXPO_PUBLIC_` para lo que necesite el cliente: `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY`, `EXPO_PUBLIC_REVENUECAT_API_KEY`.
- Preferir **tiers gratis/sandbox** mientras se prueba, antes de comprometerse a un plan pago:
  - Supabase: tier gratis alcanza para desarrollo (Auth + Postgres + Storage).
  - RevenueCat: gratis hasta cierto volumen de ingresos + modo sandbox para probar compras sin cobrar de verdad.
  - Tiempo real: Colyseus/Socket.io son open source, sin costo de licencia (el costo es solo del hosting).
  - Voz (WebRTC): LiveKit tiene tier gratis / self-hosted; Agora también tiene tier gratis con límite de minutos — evaluar cuál conviene cuando se llegue al Paso C.
- Si algún servicio termina siendo de pago, avisar antes de asumir el costo y buscar la alternativa gratis equivalente para la fase de pruebas.

## Convenciones de trabajo
- Un repo para este proyecto (no mezclar con otras apps).
- Al final de cada sesión (o tras un cambio completo), commitear y pushear a `origin/main` sin pedir confirmación cada vez, salvo que quede algo a medio terminar o `git status` muestre algo raro.
- Actualizar este `CLAUDE.md` (sección "📌 Decisiones clave" y "✅ Próximos pasos") con cada avance importante.
- **Reflejar cada avance importante en Obsidian**: hay una nota espejo de este proyecto en el "segundo cerebro" del usuario, en `C:\Obsidian Vault\proyectos\tu-espacio.md`. Si Claude Code tiene acceso a esa carpeta, actualizarla junto con este archivo (misma lógica que el resto de los proyectos del usuario: decisiones clave fechadas, próximos pasos con checkboxes). Si no tiene acceso, dejarlo anotado como pendiente de sincronizar manualmente.

## 📌 Decisiones clave
- **2026-07-27:** Proyecto iniciado a partir del documento de arquitectura subido (`Tu_Espacio_Documento_Completo_v3.pdf`). Se armó este `CLAUDE.md` con stack, esquema de Supabase, modelo de monetización/permisos y los 5 prompts secuenciales (Pasos A-E) como roadmap. Se creó la nota espejo en Obsidian (`proyectos/tu-espacio.md`). Sin credenciales reales todavía — se usarán tiers gratis/sandbox de Supabase y RevenueCat para las pruebas iniciales.
- **2026-07-27:** Node.js no estaba instalado en el equipo y no había `winget`/`choco` disponibles para instalarlo, y el instalador MSI oficial falló por falta de permisos de administrador (Error 1925). Se instaló la versión **portable** de Node.js v24.18.0 LTS en `%LOCALAPPDATA%\Programs\nodejs-portable\` (sin necesitar admin) y se agregó al PATH de usuario. Si en una terminal nueva `node`/`npm` no se reconocen, puede requerir cerrar sesión de Windows una vez para que el PATH nuevo se propague, o agregar esa carpeta al PATH manualmente.
- **2026-07-27:** Se inicializó el repo git local (rama `main`). Todavía no tiene remoto configurado en GitHub — falta correr `git remote add origin <url>` con el repo que se cree para este proyecto antes de poder pushear.
- **2026-07-27:** **Paso A completado** (auth + roles). Se scaffoldeó la app con `create-expo-app` (blank-typescript, SDK 57). Se agregó `@supabase/supabase-js` + AsyncStorage para persistencia de sesión, y `@react-navigation` (native-stack) para el ruteo. `src/contexts/AuthContext.tsx` maneja sesión y perfil; `src/navigation/RootNavigator.tsx` decide la pantalla según estado: sin sesión → `LoginScreen`, `role === 'cliente'` → `ClienteScreen`, `role === 'curador'` o `'super_admin'` → `CuradorScreen` (con panel básico de creación de salas, ya conectado a una tabla `salas_3d`). La migración SQL vive en `supabase/schema.sql` (tabla `profiles` con enum de rol + trigger que crea el perfil automáticamente al registrarse, tabla `salas_3d` con RLS). Validado con `tsc --noEmit` (sin errores) y `expo export --platform web` (bundle de 524 módulos sin errores).
- **2026-07-27:** Repo conectado a GitHub (`https://github.com/STpipa/Tu-Espacio.git`) y pusheado a `origin/main`.
- **2026-07-27:** Proyecto real de Supabase creado (`xhhzkxplvwuybgksugxg`, tier free). `.env` cargado con las credenciales reales (`EXPO_PUBLIC_SUPABASE_URL` + `EXPO_PUBLIC_SUPABASE_ANON_KEY` usando la `publishable key`, que es el nuevo nombre de la `anon key`). La `secret key` del proyecto **no** se usa ni se guarda en el repo — es solo para backend/servidor, nunca para el cliente. Se corrió `supabase/schema.sql` en el SQL Editor.
- **2026-07-27:** **Paso A probado end-to-end por el usuario**: registro sin confirmación de email (se desactivó "Confirm email" en Supabase para agilizar pruebas — revisar si se reactiva antes de producción), cambio manual de rol a `curador` desde el Table Editor, panel de curador visible con el botón de creación de sala. Pendiente (anotado por el usuario, no urgente): mejorar el estilo visual del dashboard, hoy es muy plano — no es prioridad ahora, se retoma más adelante.
- **2026-07-27:** **Paso B completado y probado en versión web.** Se agregó `expo-gl` + `three` + `@react-three/fiber` para el entorno 3D. `src/three/Sala3D.tsx` renderiza una sala con piso, luces y un avatar (`AvatarModel.tsx`), con cámara orbitable 360° manejada a mano con `PanResponder` (`useOrbitCamera.ts`: un dedo/mouse rota, dos dedos hacen pinch-zoom — el pinch todavía no tiene equivalente de mouse en la versión web, queda pendiente probarlo en celular). `AvatarEditorScreen.tsx` es la pantalla nueva (accesible desde Cliente y Curador con el botón "Personalizar mi avatar"): selector por categoría (capa/disfraz/accesorio) con flechas, botón "Al azar", botón "Guardar" que persiste en `profiles.avatar_config`, y un D-pad para mover el avatar en el piso. Migración nueva en `supabase/schema_paso_b.sql` (tabla `catalogo_avatares` con 8 filas semilla). **Importante:** todavía no hay modelos `.glb` reales subidos a Supabase Storage — `model_url` queda `NULL` en el catálogo, así que los avatares se ven como cápsulas de colores (color derivado determinísticamente del nombre) en vez de los diseños reales (Superman, rana, martillo de Thor, etc). La arquitectura ya soporta cargar modelos reales cuando se suban; por ahora es placeholder a propósito. Probado por el usuario en navegador (web): funciona y el usuario confirmó que "anda estable" y "le gusta el diseño". **Falta probar en celular** (Expo Go) — pendiente, se hace más adelante.
- **2026-07-27:** **Paso C completado y probado (posición en tiempo real + moderación).** Primer componente de backend propio del proyecto además de Supabase: carpeta `server/` con un servidor **Colyseus** (Node + TypeScript, independiente del proyecto Expo — tiene su propio `package.json`). Versión fijada a la línea `0.16.x` de Colyseus a propósito: el cliente `colyseus.js` todavía no tiene una versión `0.17` publicada, así que usar la versión "latest" del server (`0.17.x`) habría roto el protocolo cliente-servidor.
  - `server/src/rooms/SalaRoom.ts`: valida el JWT de Supabase de cada conexión (`onAuth`), carga el rol real desde `profiles` (nunca confía en un rol mandado por el cliente), sincroniza posición/rotación (`mensaje "mover"`), y expone moderación (`mensaje "moderar"`: congelar/descongelar/silenciar/habilitar/teletransportar/expulsar) solo si `role` es `curador` o `super_admin`.
  - Cada sala de Supabase (`salas_3d.codigo_acceso`) mapea a una instancia aislada de Colyseus vía un endpoint HTTP propio (`POST /salas/resolver`, usando `matchMaker.query`/`matchMaker.createRoom` de `@colyseus/core`) — así dos salas distintas nunca comparten jugadores.
  - **Bug encontrado y arreglado:** la política RLS de `salas_3d` solo dejaba ver la sala a su propio curador, así que un cliente uniéndose por código no podía leerla (`onAuth` fallaba con "La sala no existe"). Se arregló con una función `SECURITY DEFINER` (`buscar_sala_por_codigo`, en `supabase/schema_paso_c.sql`) que devuelve solo la fila que coincide exacto con el código pedido, sin abrir la tabla entera a lectura pública (eso hubiera permitido listar todos los códigos de todas las salas).
  - Cliente: `colyseus.js` (fijado a `0.16.22` para que coincida con el server), `src/hooks/useSalaRoom.ts` maneja la conexión y el estado sincronizado (usa `getStateCallbacks`, el patrón nuevo de callbacks de Colyseus 0.16+, ya no `.onAdd` directo sobre el schema), `src/screens/SalaEnVivoScreen.tsx` es la pantalla en vivo con el D-pad de movimiento y el panel de moderación (solo visible para curador/super_admin).
  - Validado con un script de prueba automatizado (2 clientes simulados con cuentas de Supabase reales) antes de pasarlo al usuario: sincronización de posición ✅, un cliente sin rol de curador no puede moderar ✅, congelar funciona ✅, expulsar funciona ✅.
  - Probado a mano por el usuario con dos ventanas de navegador (una normal + una de incógnito, porque dos pestañas normales comparten sesión): funcionó, vio los dos avatares, la moderación, y reportó que las flechas de movimiento quedaban tapadas por el panel de jugadores — se arregló (las flechas ahora están dentro de su propio contenedor `flex:1` en vez de posicionarse relativas a toda la pantalla).
  - **Incidente:** en un momento los dos servidores de desarrollo (Expo web y Colyseus) quedaron colgados sin escuchar en su puerto (probablemente por correr en ventanas de PowerShell separadas y desatendidas cuya salida no se podía inspeccionar). Desde entonces se corren como procesos en segundo plano con logs legibles en vez de ventanas nuevas — más fácil de diagnosticar si vuelve a pasar.
  - **Todavía no implementado (fuera del alcance de este paso, a propósito):** audio real y chat de texto — "silenciar" hoy es solo una bandera de estado (`silenciado`) sin ningún pipeline de audio/WebRTC detrás, lista para conectarse cuando se decida entre LiveKit/Agora (ver sección de stack). Tampoco hay reconexión automática si se corta la conexión a Colyseus.
- **2026-07-27:** **Paso D completado y probado (monetización + bypass de pagos), decisión: RevenueCat se cablea de código ya, cuenta real queda para después.**
  - **Fix de seguridad primero (prerrequisito real, no cosmético):** durante las pruebas del Paso C se detectó que la RLS de `profiles` dejaba que cualquier usuario cambiara su propio `role`/`exento_pago` desde el cliente — si no se arreglaba, todo el gating de este paso hubiera sido trivial de saltear. Se restringió a nivel de columna (`revoke update ... grant update (avatar_config) ...`, en `supabase/schema_paso_d.sql`), verificado con un test que confirma `permission denied` al intentar auto-ascenderse, y que `avatar_config` se sigue pudiendo editar normalmente.
  - `src/lib/monetizacion.ts`: `puedeCrearSala()` (super_admin, exento_pago, o suscripción activa), `puedeUnirseComoCliente()` (bloquea a los `cliente` sin exención después de 2 asistencias distintas en el mes), `registrarAsistenciaSiHaceFalta()` marca la asistencia del día (idempotente vía `unique(cliente_id, fecha_asistencia)` en la tabla nueva `sesiones_uso`).
  - Conectado en `CuradorScreen` (chequeo antes de crear sala) y `ClienteScreen` (chequeo antes de unirse, más un contador "sesiones usadas este mes: X/2" visible). La asistencia se registra automáticamente al conectar de verdad a una sala (dentro de `useSalaRoom`), no al tocar el botón — así un código inválido no gasta una sesión.
  - **RevenueCat:** el usuario eligió construir toda la lógica ahora y dejar la cuenta/SDK real para después. `src/lib/revenuecat.ts` es un módulo placeholder documentado (`verificarSuscripcionActiva()` siempre devuelve `false` por ahora) — **a propósito NO se instaló** `react-native-purchases` todavía, porque es un módulo nativo: instalarlo rompe la posibilidad de seguir probando con Expo Go/navegador tal cual veníamos haciendo, y pasa a necesitar un dev client propio (`expo prebuild` + EAS Build o build local). Cuando el usuario tenga cuenta de RevenueCat y quiera probarlo de verdad, activar esto es: instalar el paquete, configurar el entitlement, y reemplazar el cuerpo de esa única función.
  - Validado con un script de prueba contra la base real (fix de seguridad, `avatar_config` sigue editable, `sesiones_uso` respeta el límite diario) + `tsc --noEmit` + `expo export --platform web`, todos sin errores.
- **2026-07-27:** **Paso E completado y probado (permisos, onboarding y preparación para EAS) — cierra el roadmap original de 5 pasos.**
  - `src/lib/permisos.ts`: `pedirPermisoGaleria/Camara/Microfono/Notificaciones`, cada uno con un `Alert.alert` explicativo antes de pedir el permiso real (`expo-image-picker`, `expo-audio`, `expo-notifications`).
  - `src/lib/fotoPerfil.ts` + `src/components/FotoPerfil.tsx`: flujo completo elegir→subir→guardar. Sube a un bucket nuevo de Storage (`fotos-perfil`, ruta `<userId>/foto.jpg`) y guarda la URL pública (con cache-busting `?v=timestamp`) en `profiles.foto_url` (columna nueva, migración `supabase/schema_paso_e.sql`, que extiende el `grant update` de columnas de la Paso D a `(avatar_config, foto_url)` — sigue sin poder tocar `role`/`exento_pago` desde el cliente).
  - `src/components/PermisosCard.tsx`: tarjeta con botones para pedir micrófono (preparado para cuando haya audio real vía WebRTC) y notificaciones push (pide el token, todavía no se envía nada real — falta un backend que dispare notificaciones, fuera de alcance de este paso).
  - `src/screens/OnboardingScreen.tsx`: 3 pantallas (Sanación y Conexión / Espacios 3D e Innovación / Sesiones Guiadas en Vivo), se marca como visto en `AsyncStorage` (`src/lib/onboarding.ts`) y `RootNavigator` lo chequea antes que la sesión.
  - `app.json`: plugins de `expo-audio` (permiso de micrófono), `expo-image-picker` (galería + cámara) y `expo-notifications`, cada uno con su mensaje explicativo — ya queda listo para build con EAS.
  - **Bug real encontrado y arreglado (no cosmético):** `Alert.alert` de `react-native-web` es un no-op literal (`class Alert { static alert() {} }`) — no muestra nada y nunca llama a los botones. Como `pedirConfirmacion()` en `permisos.ts` envolvía el `Alert.alert` en una `Promise` que solo se resuelve cuando se aprieta un botón, en la versión web esa promesa nunca se resolvía: el usuario reportó que al tocar "Editar foto" la rueda de carga quedaba girando para siempre. Mismo bug encontrado de paso en el botón "Expulsar" del panel de moderación (`SalaEnVivoScreen.tsx`) — ahí no colgaba nada, pero el botón no hacía nada silenciosamente. Fix: en ambos lugares, si `Platform.OS === "web"` se usa `window.confirm(...)` (sincrónico, dispara solo) en vez de `Alert.alert`. Queda como precedente: cualquier `Alert.alert` nuevo que dependa de que se apriete un botón necesita el mismo chequeo de plataforma para funcionar en la versión web.
  - **Incidente de "pantalla en blanco" (segunda vez esta sesión, causa distinta a la primera):** después de instalar `expo-audio`/`expo-image-picker`/`expo-notifications`/`base64-arraybuffer`, Metro quedó con la caché de resolución de módulos stale y tiraba `Unable to resolve "expo-audio"` aunque el archivo ya existía en disco. Se arregló matando los procesos de Node colgados y reiniciando ambos servidores de desarrollo con `npx expo start --web --clear` (cache de Metro purgada). Confirmado por log limpio de bundling (718 módulos, sin errores).
  - Validado con `tsc --noEmit` sin errores, bundle web limpio, y prueba manual completa del usuario en el navegador: onboarding, subida de foto de perfil, y los botones de micrófono/notificaciones de la tarjeta de permisos — todos funcionando después del fix de `Alert.alert`.
- **2026-07-28:** **Identidad visual "constelar" (violeta/lila) aplicada a toda la app**, a partir de un mockup en Artifact que el usuario aprobó (paleta + tipografía + una figura 3D hecha de constelaciones) — se le agregaron más estrellas de fondo y una figura en 3D real a pedido suyo antes de portarlo al código.
  - `src/lib/theme.ts`: paleta nueva (`background #0F0919`, `primary #9D5CFF`, `primarySoft #D8B4FE`, `warm #E4B764` para el rol de curador, etc.) + `gradients` (para botones/anillos) + `fonts.display` (Cinzel).
  - Tipografía real: se instaló `@expo-google-fonts/cinzel` + `expo-font` (config plugin en `app.json`) y se cargan las dos variantes usadas (`Cinzel_600SemiBold`/`700Bold`) en `App.tsx` antes de montar la app (pantalla en blanco con el color de fondo mientras cargan).
  - `expo-linear-gradient` para los botones en degradé y el anillo tipo halo alrededor de la foto de perfil (`FotoPerfil.tsx`).
  - Todas las pantallas (Login, Onboarding, Cliente, Curador, Editor de avatar, Sala en vivo) y componentes compartidos (`PermisosCard`, `MovementPad`, `LoadingScreen`, `RootNavigator`) actualizados a la paleta nueva; `RootNavigator` ahora usa un `theme` de React Navigation basado en `DarkTheme` en vez del tema claro por defecto.
  - **Las salas 3D (`Sala3D.tsx`, `SalaMultiplayer3D.tsx`) pasaron de un piso/cielo color crema a un ambiente cósmico oscuro** (fondo y piso violeta, niebla, luz puntual violeta) con un campo de estrellas animado nuevo (`src/three/Starfield.tsx`, hecho a mano con `@react-three/fiber` puro — sin agregar `@react-three/drei` — que rota lento y admite dos modos: `dome` para las salas, con estrellas solo por encima del piso, y `sphere` para el hero del login, con estrellas alrededor en todas direcciones).
  - **Figura constelar 3D en el login (`src/three/ConstellationHero.tsx`)**: un esqueleto de 15 puntos/13 huesos en pose abierta (la misma que el mockup aprobado) que gira con `useFrame`, con un halo detrás hecho con dos `sprite` (billboards que siempre miran a cámara) usando una textura de resplandor generada a mano con `THREE.DataTexture` — **a propósito no se usó un `<canvas>` del DOM para generar el gradiente** porque React Native no tiene esa API (rompería en celular), así que se calculó el degradé radial pixel por pixel en un `Uint8Array`. `LoginScreen.tsx` se reestructuró con un `ScrollView` y la escena 3D como header fijo (260→300px) en vez de fondo absoluto detrás del formulario, para no arriesgar legibilidad del texto sobre una animación.
  - **Iteración con el usuario:** primera versión tenía la cabeza de la figura cortada arriba del canvas (la cámara miraba derecho por su eje -z en vez de apuntar al centro de la figura) y el halo se veía "rústico" (esferas 3D lisas en vez de un degradé de nube). Se arregló apuntando la cámara explícitamente con `camera.lookAt(...)` vía un componente `CameraAim` (con `useThree`), alejándola un poco y bajando el FOV, y reemplazando las esferas por los sprites con textura de resplandor.
  - **Incidente de "WebGLRenderer: Context Lost"** en el log del navegador durante el hot-reload de estos cambios — no rompió nada, es normal cuando Fast Refresh reinicia una escena de Three.js en caliente; se le pidió al usuario hacer un refresh completo de la pestaña para curarlo si volvía a pasar.
  - Validado con `tsc --noEmit` y bundle web limpios en cada iteración; el usuario confirmó el look final por navegador antes de seguir.
- **2026-07-28:** **Arranque de los puntos 1 a 6 pendientes** (el usuario pidió avanzar en los seis a la vez). Antes de tocar código en los que implican una decisión de producto o cuenta externa, se le preguntó con opciones concretas: eligió **Kenney (CC0, gratis, sin cuenta)** para los modelos de avatar, **dejar el audio real (WebRTC) para después** (es el cambio más grande de los seis, rompe Expo Go), y **Colyseus Cloud** para el hosting del server.
  - **Punto 1 (probar en celular):** `src/lib/colyseus.ts` ahora detecta la IP de LAN sola en nativo (vía `Constants.expoConfig?.hostUri` de `expo-constants`, recién agregado como dependencia explícita) en vez de necesitar cambiar `EXPO_PUBLIC_COLYSEUS_URL` a mano cada vez que se prueba desde el teléfono. El server de Colyseus ya escuchaba en todas las interfaces de red (no hacía falta tocarlo). Sigue pendiente la prueba real en un celular (Windows Firewall puede pedir permiso la primera vez que el teléfono se conecte al puerto 2567 — avisado, no resuelto de antemano).
  - **Punto 2 (modelos `.glb` reales):** se investigaron packs CC0 de Kenney; los que tienen variedad temática real (ninja, monje, rana, capa, martillo, alas, espada) resultaron ser todos de sprites 2D, no 3D. Se optó por un v1 honesto: 4 cuerpos humanos reales del pack **Kenney "Mini Characters"** (CC0) para `Traje Clásico`/`Superman`/`Ninja`/`Monje` — no son disfraces temáticos exactos, son cuerpos base reales en vez de cápsulas, mejor que nada pero **todavía no calzan con el nombre** (ej. "Ninja" no se ve como ninja). `Rana` y los 3 accesorios (`Martillo de Thor`, `Alas de Ángel`, `Espada Mística`) siguen con el placeholder geométrico simple por no encontrarse un asset CC0 razonable — pendiente para una sesión dedicada a curar/conseguir assets mejores.
    - Detalle técnico: los `.glb` de Kenney en este pack referencian su textura (`colormap.png`) como archivo externo en vez de embebida — se usó `@gltf-transform/cli` (`npx`, no quedó como dependencia del proyecto) para incrustar la textura y dejar cada modelo como un único archivo autocontenido antes de subirlo.
    - `src/three/AvatarModel.tsx` reescrito: cuando `capa`/`disfraz` tiene `model_url`, carga el `.glb` real con `GLTFLoader` de three.js (`useLoader`, dentro de `Suspense`); si no hay URL o falla la carga (`LimiteDeError`, un error boundary de clase), cae de vuelta a la cápsula de siempre — nunca puede tirar abajo la sala entera por un modelo roto.
    - `AvatarSeleccion`/`AvatarConfig` (`src/lib/types.ts`) ahora incluyen `model_url`. `AvatarEditorScreen.tsx` lo propaga al guardar. Para los OTROS jugadores en `SalaMultiplayer3D.tsx` (que solo reciben el nombre por Colyseus, no la URL — no se tocó el server ni el schema de sincronización para esto), se resuelve el `model_url` contra el mismo catálogo vía un helper nuevo con caché simple (`src/lib/catalogoAvatares.ts`).
    - Falta un paso manual antes de que esto se vea en la app: correr `supabase/schema_paso_f.sql` (bucket de Storage `modelos-avatares`, público de lectura) y avisar para correr el script de subida (`scratch-subir-modelos.mjs`, no comiteado) que devuelve el `UPDATE` SQL con las URLs reales para pegar en el SQL Editor.
  - **Punto 4 (hosting Colyseus):** se agregó `.colyseus-cloud.json` al `.gitignore` de `server/` (por si se retomaba Colyseus Cloud, mismo criterio que `.env`). **Corrección importante:** Colyseus Cloud en realidad **no tiene tier gratis** (arranca en 15 USD/mes, se verificó en `colyseus.io/pricing` después de que el usuario lo probara y le pidiera pago). Alternativa gratis propuesta: **Render.com** (tier free real, soporta WebSockets, 750 hs/mes, sin tarjeta, con la contra de que el server se duerme a los 15 min de inactividad y tarda ~1 min en despertar de nuevo). `server/src/index.ts` ya está listo para Render tal cual (usa `process.env.PORT`, tiene un healthcheck en `/`) — falta que el usuario cree la cuenta y decida si conviene ahora o seguir en local mientras se prueba.
  - **Puntos 5 y 6 (RevenueCat real / reactivar Confirm Email):** sin cambios de código posibles sin que el usuario actúe primero — crear la cuenta de RevenueCat (confirmado que el tier gratis es real, hasta 2500 USD/mes de ingresos trackeados, sin tarjeta para arrancar), y en el caso de Confirm Email es un toggle en el dashboard de Supabase (Authentication → Providers → Email) al que no se llega con la anon key.
- **2026-07-28:** **Zoom con rueda del mouse + 5 entornos 3D dinámicos, con el cambio de entorno restringido al curador y sincronizado para toda la sala.**
  - `src/three/useOrbitCamera.ts`: nuevo `containerRef` con un listener nativo de evento `wheel` (solo en web, `PanResponder` no tiene concepto de rueda) que ajusta el mismo `radius` que ya usaba el pellizco de dos dedos (clamp 2.5–12).
  - `src/three/Environment.tsx` (nuevo): 5 entornos completos con elementos animados a mano (sin imágenes/video, todo geometría three.js vía `@react-three/fiber`) — **Noche constelar** (el original), **Paraíso** (cielo cálido, arena, palmeras, partículas doradas subiendo), **Infierno** (grietas de lava con parpadeo, brasas subiendo), **Tierra** (cielo de día, pasto, nubes cruzando), **Bajo el mar** (piso de arena, cardumen de ~11 peces de colores nadando en círculos, burbujas subiendo). Componentes reutilizables compartidos entre entornos (`Particulas`, `Nube`/`Cielo`, `Pez`/`Cardumen`, `Palmera`, `Lava`).
  - `src/components/EnvironmentPicker.tsx` (nuevo): fila de chips horizontal para elegir entorno.
  - **Decisión del usuario tras probarlo:** el control de fondo/entorno debe ser exclusivo del curador (no de cada usuario por separado), y debe verse igual para todos en la sala — así que se sincronizó vía Colyseus en vez de dejarlo como estado local de cada pantalla. `SalaState.ts` (server) ganó un campo `entorno` (`@type("string")`, default `"noche"`); nuevo mensaje `cambiarEntorno` en `SalaRoom.ts` que solo aplica el cambio si `role` es `curador`/`super_admin` y el valor está en una lista blanca (`ENTORNOS_VALIDOS` en `server/src/types.ts`) — nunca confía en lo que mande el cliente. `useSalaRoom.ts` escucha ese campo (`$(room.state).listen("entorno", ...)`, el patrón de Colyseus 0.16 para propiedades primitivas, no colecciones) y expone `entorno`/`cambiarEntorno`. `SalaEnVivoScreen.tsx` solo muestra el `EnvironmentPicker` si `soyModerador`, pero todos los jugadores reciben y ven el mismo `entorno` sincronizado.
  - En el **editor de avatar** (`AvatarEditorScreen.tsx`), que es un espacio personal en solitario (no hay otros jugadores viéndolo), el `EnvironmentPicker` se dejó sin restringir — cualquier usuario puede probar fondos ahí libremente, no afecta a nadie más.
  - Validado con `tsc --noEmit` limpio (server y app) y reinicio en caliente de `tsx watch` (server) confirmado por log sin errores; bundle web forzado sin errores. Pendiente que el usuario lo pruebe con dos ventanas (una curador, una cliente) para confirmar la sincronización en vivo.
- **2026-07-28:** **Bug real encontrado antes de la prueba en celular: `.env` tenía `EXPO_PUBLIC_COLYSEUS_URL=ws://localhost:2567` fijado a mano**, que pisaba por completo la detección automática de IP de LAN agregada en el punto 1 (esa variable tiene prioridad sobre `endpointPorDefecto()` en `src/lib/colyseus.ts`). En el teléfono "localhost" apunta al propio teléfono, no a esta PC, así que la conexión a Colyseus hubiera fallado en la primera prueba real. Se sacó la línea del `.env` (queda comentado el motivo) y se reinició el server de Expo para que tome el cambio — confirmado en el log que ya no exporta `EXPO_PUBLIC_COLYSEUS_URL`, solo las dos de Supabase.
- **2026-07-28:** **`render.yaml` agregado en la raíz del repo**, blueprint de Render.com para el server de Colyseus (`rootDir: server`, `npm install && npm run build` / `npm start`, plan free, con `SUPABASE_URL`/`SUPABASE_ANON_KEY` marcadas `sync: false` para cargarlas a mano en el dashboard de Render en vez de commitearlas). Verificado localmente que `npm run build` genera `dist/index.js` como espera `npm start`. Deploy real queda pendiente de que el usuario cree la cuenta en Render — con este archivo alcanza con conectar el repo de GitHub como "Blueprint" y pegar las dos env vars.
- **2026-07-28:** **Server de Colyseus desplegado de verdad en Render** (`https://tu-espacio-server.onrender.com`, tier free). Validado con `curl`: `/` devuelve el healthcheck ok, y `POST /salas/resolver` crea una sala vía matchmaking (el chequeo real de que el código exista pasa después, en `onAuth`, como estaba diseñado). `.env` de la app actualizado: `EXPO_PUBLIC_COLYSEUS_URL=wss://tu-espacio-server.onrender.com` (nota el esquema `wss` en vez de `https` — `httpEndpoint()` en `src/lib/colyseus.ts` lo convierte solo). **Trade-off importante que queda documentado en el propio `.env`:** con esa variable seteada, TODO cliente (web y celular) habla con Render, no con el server local — si se va a tocar código de `server/` conviene comentar esa línea temporalmente y probar contra `cd server && npm run dev` en caliente (`tsx watch`), porque contra Render cada cambio requeriría pushear y esperar el redeploy. Server de Expo reiniciado y confirmado en el log que ahora exporta `EXPO_PUBLIC_COLYSEUS_URL`; bundle web forzado sin errores.
- **2026-07-28:** **`render.yaml` extendido con un segundo servicio (`tu-espacio-web`)**, sitio estático de Render (`runtime: static`, `npx expo export --platform web`, `staticPublishPath: ./dist`) para tener una URL pública de la app en sí, además del server. No se duerme por inactividad (a diferencia del server de Colyseus, que sí). Env vars públicas (`EXPO_PUBLIC_*`) cargadas como `sync: false` en el dashboard, igual que el otro servicio.
- **2026-07-29:** **`tu-espacio-web` confirmado desplegado, pero quedaba en blanco — bug real, no de código.** El usuario abrió la URL del sitio estático y no vio nada. Diagnóstico: se descargó el bundle JS publicado en Render y se comparó contra el bundle local — el de Render no tenía embebidas ni `EXPO_PUBLIC_SUPABASE_URL` ni `EXPO_PUBLIC_COLYSEUS_URL` (solo la anon key), porque esas env vars son `sync: false` en el `render.yaml` (a propósito, para no commitear nada) y hay que cargarlas a mano una vez en el dashboard — el usuario no lo había hecho todavía para este segundo servicio. Como esas variables se hornean en el bundle en tiempo de build (no en runtime), la app crasheaba al arrancar el cliente de Supabase con una URL vacía, antes de poder pintar nada — pantalla en blanco sin ningún error visible fuera de la consola del navegador. Se lo guio a cargar las 3 variables en Render → servicio `tu-espacio-web` → "Environment" → "Environment Variables" (no "Secret Files" ni "Linked Environment Groups", que también aparecen ahí y confunden), con los mismos valores que ya tenía el `.env` local. Confirmado arreglado por el usuario.
- **2026-07-29:** **Servidores de desarrollo locales caídos tras quedar la PC inactiva de un día para el otro** — ni el server de Colyseus (puerto 2567) ni Expo (puerto 8081) estaban escuchando. Se reiniciaron ambos sin drama.
- **2026-07-28:** **Motor de partículas reactivas y campos ambientales — etapa 1 (el motor en sí, todavía no integrado a la sala multiusuario real).** El usuario trajo un prompt propio muy ambicioso (partículas GPU, compute shaders, `glslify`, `@react-three/drei`, 5 entornos arquetípicos nuevos). Antes de escribir código se tradujo a lo que esta pila (Expo + Metro + expo-gl, sin soporte de compute shaders reales ni de glslify) puede sostener de verdad, y se le preguntó al usuario cómo debían convivir los 5 campos nuevos con los 5 "Entornos" que ya existen (`Environment.tsx`) — **eligió agregarlos como grupo aparte, no reemplazar**.
  - **Decisiones técnicas (no negociables dado el stack):** sin `glslify` (Metro no lo soporta sin un transformer custom) — los shaders son strings de JS pasados a `<shaderMaterial>`, el patrón estándar de R3F. Sin `@react-three/drei` (el proyecto lo evita desde el Paso B por riesgo de romper nativo/nativo-Expo-Go) — todo hecho a mano, mismo criterio que `Starfield`/`Environment`. Sin compute shaders reales (WebGL clásico no los tiene) — la simulación de física corre en CPU sobre `Float32Array` planos y se sube a GPU solo el buffer de posición cada frame (`needsUpdate`), que es como se hacen sistemas de esta escala (miles, no millones, de partículas) en three.js en la práctica. El ruido de turbulencia (`src/three/particles/noise.ts`) es una suma de senos/cosenos a propósito, no Simplex/Perlin real — más barato y sin riesgo de portar mal a mano un algoritmo que es fácil de arruinar sutilmente. Cantidad de partículas adaptativa por plataforma (7000 en web, 2500 en nativo) en vez de un número fijo, pensando en celulares gama media.
  - **`zustand`** sí se agregó como dependencia nueva (liviana, sin nada nativo/DOM-específico, segura para nativo) para el store pedido (`src/three/particles/particleStore.ts`): `setCampoAmbiental`, `addRepresentanteFuerza`, `updateRepresentantePosition`, `removeRepresentanteFuerza`, `conectarRepresentantes`, `desconectarRepresentantes`.
  - `src/three/particles/`: `ParticleSystemManager.tsx` (simulación + `Points` con shader custom — atracción/repulsión-con-vórtice/turbulencia, cada partícula con posición/velocidad/carga energética -1..1 que tiñe su brillo), `ConnectionThreads.tsx` (tubos con shader de flujo animado entre dos representantes conectados, color según `tipoVinculo`: amor/trauma/secreto, este último con parpadeo), `camposAmbientales.ts` (los 5 configs: Vientre materno, Horizonte de agujero negro, Paraíso/Campo de luz, Infierno/Submundo, Espacio profundo — el agujero negro registra una fuerza de atracción central automáticamente), `CampoAmbientalRenderer.tsx` (fondo/niebla/luces/piso por config, con transición lineal de 2s pedida explícitamente en el prompt original, más un piso de grilla deformada a mano para el agujero negro).
  - `src/components/CampoPicker.tsx` + `src/screens/ParticleLabScreen.tsx`: demo aislada nueva, accesible desde el panel de curador — hasta 2 "representantes" (D-pad para moverlos), cada uno con su tipo de energía, más el toggle para conectarlos con un vínculo. **(Nota: esta demo se borró al otro día, ver entrada siguiente — quedó solo como paso intermedio de validación.)**
  - Etapa 1 deliberadamente aislada de la sala real, para probar el motor en sí antes de cablear representantes reales y sincronización multijugador. Validado con `tsc --noEmit` limpio y bundle de Metro sin errores (750 módulos, +32 nuevos).
- **2026-07-29:** **Etapa 2 del motor de partículas: integrado a la sala real, demo borrada.** El usuario probó la demo aislada, le gustó, pero pidió explícitamente que viva **dentro de la sala en vivo** (no aparte) y que el curador controle el ambiente ahí mismo — igual que ya podía hacer con los 5 "Entornos" clásicos.
  - **Se borró** `src/screens/ParticleLabScreen.tsx` y `src/components/CampoPicker.tsx` (ya no hacían falta), y la ruta/botón correspondientes en `RootNavigator.tsx`/`CuradorScreen.tsx`.
  - **Unificación de ambiente:** el campo sincronizado por Colyseus se renombró de `entorno` a `ambiente` en todo el pipeline (`SalaState.ts`, mensaje `cambiarEntorno` → `cambiarAmbiente`, `server/src/types.ts` con `AMBIENTES_VALIDOS` ampliada a los 10 ids — los 5 Entornos + los 5 Campos — en vez de solo 5) y en el cliente (`useSalaRoom.ts`). `src/lib/ambientes.ts` (nuevo) define el tipo unión `AmbienteId` y la lista combinada `AMBIENTES` para el picker; `src/components/AmbientePicker.tsx` (nuevo, reemplaza a `EnvironmentPicker` **solo en la sala en vivo** — el editor de avatar individual sigue usando `EnvironmentPicker`/solo los 5 Entornos, sin cambios) muestra los 10 en una sola fila de chips, con "✨" en el nombre de los que traen el motor de partículas.
  - **`SalaMultiplayer3D.tsx`** decide en cada render si el `ambiente` activo es un Campo (`esCampoConMotor`) — en ese caso renderiza `CampoAmbientalRenderer` en vez de `Environment`, y un hook nuevo (`useSincronizarRepresentantesReales`) registra/actualiza/limpia en el store del motor de partículas un `RepresentanteFuerza` real por cada jugador de la sala (`avatar-<sessionId>`, posición X/Z real del avatar), con el tipo de energía que le toca a ese campo (`energiaRepresentantePorDefecto`, nuevo campo en cada config de `camposAmbientales.ts`: atracción en Útero/Paraíso, repulsión en Infierno, turbulencia en Agujero negro/Espacio profundo). Así las partículas reaccionan de verdad a dónde están y se mueven las personas reales de la sala, no a esferas de demo.
  - Todavía **no** hay UI para que el curador conecte a dos representantes con un vínculo (`conectarRepresentantes`) dentro de la sala real — esa pieza de la demo no se portó (no se pidió esta vez); si se quiere más adelante, el store ya la soporta.
  - **Incidente de caché de Metro (tercera vez en el proyecto, mismo patrón que ya pasó con `expo-audio`):** al agregar `AmbientePicker.tsx` con el server corriendo, el HMR tiró `Requiring unknown module "767"` — se resolvió, como las veces anteriores, reiniciando con `npx expo start --web --clear`.
  - Validado con `tsc --noEmit` limpio en server y app, el server local reiniciado sin errores en runtime (confirma que el rename del schema de Colyseus no rompió los decoradores), y bundle de Metro limpio tras el `--clear`.
- **2026-07-29:** **Nueva fuente de assets de personajes: itch.io (packs "LOWPO" de Standout7), y pipeline FBX→GLB agregado.** Después de la charla sobre dónde conseguir variedad temática barata y de pago único, el usuario bajó varios packs gratis de `standout7.itch.io` y los dejó en `assets-import/` (carpeta nueva, gitignoreada — son archivos fuente de terceros, no van al repo). A diferencia de los `.glb` de Kenney, estos vienen en **FBX** (no glTF), así que hizo falta una herramienta nueva: el paquete npm `fbx2gltf` (wrapper de la CLI de Meta `FBX2glTF`, usado vía el binario `bin/Windows_NT/FBX2glTF.exe` directo — el wrapper JS del paquete está roto/desactualizado para Windows, así que se invoca el `.exe` a mano) con las flags `-b --pbr-metallic-roughness` para generar un `.glb` autocontenido (textura embebida) de una sola pasada, sin necesitar el paso extra de `gltf-transform` que sí hizo falta con Kenney. Detalle no obvio: el FBX no resuelve solo la ruta a su textura (`Warning: could not find a image file for texture`) — hay que copiar el `Texture.png` del pack a la misma carpeta que el `.fbx` antes de convertir.
  - Primeros dos convertidos y subidos a Supabase Storage (mismo bucket `modelos-avatares`) para probar el pipeline de punta a punta: **Monk → reemplaza el `model_url` de "Monje"** (antes era un cuerpo genérico de Kenney sin relación con el nombre — ahora sí es un monje de verdad) y **Sword → "Espada Mística"** (una espada de fantasía genérica, no particularmente "mística", pero mejor que el placeholder geométrico). UPDATE SQL entregado al usuario para pegar en el SQL Editor de Supabase (no se puede hacer desde acá: `catalogo_avatares` solo tiene policy de `select`, ninguna de `update`, a propósito).
  - **Quedaron convertibles pero sin usar todavía** (pendiente de que el usuario decida nombres/categorías): personajes de los packs `Wizard_Free` (Wizard, Fire_Wizard, Air_Wizard), `Villager NPC Free` (Blacksmith, Child, Hunter + props Villager_Hammer/Knife/Stick — el "Villager_Hammer" es una herramienta mundana, NO se lo etiquetó como "Martillo de Thor" para no generar una expectativa falsa), `Skeletons_Free` (Skeleton, Skeleton_Archer + props), `Adventure_Free` (Adventurer, Healer + Staff_Of_Restoration/Monk_Spade) y los 3 personajes de `Crowd_Runner_Free` (más genéricos, estilo juego de carreras, menor prioridad). Assets `.fbx` originales quedan en `assets-import/` en la PC del usuario para cuando se decida cuáles sumar.
- **2026-07-29:** **Se suma TODO el resto de lo que había en `assets-import/` (21 modelos en total contando Monje/Espada) — el usuario pidió explícitamente sumarlos, no reemplazar los 8 items originales del catálogo.** 19 conversiones más vía el mismo pipeline FBX2glTF: 11 personajes nuevos como `disfraz` (Aventurero, Sanador, Jefe Corredor, Rival Corredor, Corredor, Esqueleto, Esqueleto Arquero, Herrero, Niño, Cazador, Mago del Aire, Mago del Fuego, Mago) y 8 props nuevos como `accesorio` (Pala del Monje, Báculo de Restauración, Hueso, Flecha de Hueso, Báculo de Esqueleto, Martillo de Aldeano, Cuchillo de Aldeano, Palo de Aldeano — otra vez, "Martillo de Aldeano" y no "de Thor", mismo criterio de no prometer de más). Se excluyeron a propósito los assets de `Crowd_Runner_Free/FBX/{Environment,Interactive,Obstacles}` (pistas, portones, trampas) por no ser relevantes para un catálogo de avatares.
  - **Bug real encontrado en el camino: los accesorios nunca cargaron modelo 3D real, ni con `model_url` seteado.** `AvatarModel.tsx` siempre dibujaba la cajita de relleno para `accesorio`, sin ni mirar si había una URL — quedó así desde el Paso B/punto 2 sin que nadie lo notara porque hasta ahora ningún accesorio real tenía `model_url`. Se agregó `AccesorioModeloReal` (mismo patrón de `Suspense`/`GLTFLoader`/`LimiteDeError` que ya tenía el cuerpo) y se corrigió que tanto `AvatarEditorScreen.tsx` como `SalaMultiplayer3D.tsx` propagaran el `model_url` del accesorio (antes solo mandaban `id`/`nombre`, lo pisaban en silencio).
  - Con esto, el catálogo pasa de 8 a 27 filas totales. Validado con `tsc --noEmit` y bundle de Metro limpios. INSERT SQL (19 filas) entregado al usuario para pegar en Supabase junto con el UPDATE de la entrada anterior.
- **2026-07-29:** **Accesorio ajustable a mano + el avatar ahora se puede girar 360° arrastrando con el mouse, en el editor y en la sala en vivo.** El usuario probó los modelos nuevos y reportó que el accesorio (posición fija hardcodeada, `[0.5, 0.9, 0.1]` para todos por igual) a veces atravesaba el cuerpo o tapaba la cara según el modelo — cada prop tiene un tamaño/pivote distinto, así que un solo offset para todos no podía andar bien. Antes de programar se le preguntó (a) cómo prefería ajustar el accesorio (eligió **botones**, no arrastre 3D directo — más simple y confiable) y (b) si además quería poder girar el personaje entero 360° con el mouse en ambas pantallas (confirmó que sí).
  - `AccesorioTransform` (`{offset:[x,y,z], rotacionY}`) nuevo en `AvatarConfig` (`src/lib/types.ts`), con un default que reproduce el valor viejo hardcodeado. `src/components/AccesorioAjustePanel.tsx` (nuevo): 4 filas de botones +/− (izq-der, arriba-abajo, adelante-atrás, girar) más "Reiniciar", visible en el editor de avatar solo cuando hay un accesorio elegido. Se guarda con el resto del `avatar_config` al tocar "Guardar" — no hace falta migración de Supabase, `avatar_config` ya es JSONB.
  - `src/components/RotationPad.tsx` (nuevo): un círculo separado del lienzo 3D (que ya usaba el arrastre para orbitar la cámara — por eso no podía ser el mismo gesto) que al arrastrarlo horizontalmente gira al propio avatar. En el editor es solo preview local (no se guarda, es para mirar el look desde otro ángulo). En la sala en vivo reusa el campo `rotY` de `PlayerState`, que **ya existía desde el Paso C pero nunca se usaba de verdad** (el D-pad siempre mandaba `rotY=0`, y `SalaMultiplayer3D` nunca aplicaba la rotación de otros jugadores al renderizarlos) — otro bug latente encontrado de paso.
  - Para que el accesorio ajustado se vea bien también para LOS DEMÁS jugadores de la sala (no solo en tu propia pantalla), se sincroniza por Colyseus: `PlayerState.accesorioTransform` (string JSON, mismo criterio que el resto — no se suman 4 campos numéricos nuevos al schema) cargado en `onJoin` desde `avatar_config`, parseado de vuelta en `SalaMultiplayer3D` con manejo de error si viniera corrupto (cae al default).
  - Validado con `tsc --noEmit` en server y app, reinicio del server local sin errores en runtime (confirma que el campo nuevo del schema no rompió nada), y bundle de Metro limpio. **Falta la prueba visual del usuario** (que el accesorio ya no atraviese el cuerpo, que el giro se sienta bien en ambas pantallas).
- **2026-07-29:** **Editor de avatar rediseñado con pestañas (Capa/Disfraz/Accesorio) y layout lado a lado, estilo creador de personaje de un ARPG.** El usuario probó el rediseño anterior y reportó que el panel de abajo ocupaba casi toda la pantalla, tapando el avatar, y que al mover con el D-pad "se movía el accesorio, no el personaje". Sobre esto último: se revisó el código a fondo (la posición se aplica al `<group>` que envuelve cuerpo Y accesorio juntos, así que estructuralmente mueve todo a la vez) — la hipótesis, no confirmable sin ver la pantalla del usuario, es que con todo apiñado (D-pad de movimiento, círculo de girar, y los botones de ajuste del accesorio, todos con flechas parecidas y pegados) es fácil tocar el control equivocado; **no se encontró ni se "arregló" un bug de posición porque no lo hay** — se apuesta a que separar los controles en pestañas resuelve la confusión de raíz.
  - `AvatarEditorScreen.tsx`: la escena 3D y el panel de edición ahora conviven en pestañas en vez de todo listado junto — Capa/Disfraz/Accesorio como tabs, cada una muestra solo su selector (y el ajuste de accesorio, si corresponde), no las tres a la vez.
  - **Layout responsivo** vía `useWindowDimensions()`: con `width >= 700` (heurística simple, no hay breakpoints "de verdad" en RN) el panel pasa a un costado (320px fijo, alto completo) y la escena 3D ocupa el resto — como pantallas de creación de personaje de un juego. Por debajo de ese ancho (celular) se mantiene apilado arriba/abajo como antes, pero igual se beneficia de las pestañas (mucho menos alto ocupado que listar las 3 categorías juntas).
  - **Bug real aparte, ese sí confirmado y arreglado:** en la sala en vivo y el editor, el selector de paisajes/ambientes (`EnvironmentPicker`/`AmbientePicker`) es una fila horizontal que en celular se desliza con el dedo sin problema, pero en navegador con mouse no tenía forma de moverse (la rueda vertical no mueve un `ScrollView` horizontal de react-native-web, y no soporta arrastrar con el mouse como si fuera táctil). `src/lib/useHorizontalDragScroll.ts` (nuevo hook, solo actúa en web): la rueda vertical del mouse ahora mueve el scroll horizontal (como cualquier carrusel web), y también se puede hacer click y arrastrar. Aplicado a ambos pickers.
  - Validado con `tsc --noEmit` y bundle de Metro limpios. Falta la prueba visual del usuario.
- **2026-07-29:** **Encontrado y arreglado el bug real detrás de "se mueve el accesorio pero el personaje queda rígido" — no era confusión de UI, era un bug de verdad.** El usuario insistió con un caso concreto (el "Mago" no se movía ni giraba, pero el báculo equipado sí). Se inspeccionó a mano el `.glb` del Mago (parseando el JSON embebido del binario) y se confirmó: es un modelo **riggeado** (huesos, para animación — todos los packs de itch.io convertidos vía FBX2glTF lo son). El código clonaba la escena con `gltf.scene.clone(true)`, el `.clone()` genérico de `Object3D` de three.js — y ese método **no remapea los huesos de un `SkinnedMesh`** a las copias clonadas: el mesh visible queda atado al esqueleto ORIGINAL (el que vive en el caché del loader, nunca se mueve ni gira), sin importar qué transform tenga el grupo padre. El accesorio (un mesh estático sin huesos) sí se clonaba bien y por eso se movía solo — de ahí la sensación de "se mueve una parte y la otra no". Fix: `SkeletonUtils.clone()` (`three/examples/jsm/utils/SkeletonUtils.js`, la utilidad oficial de three.js para este caso exacto) en vez de `.clone(true)`, en `CuerpoModeloReal` y `AccesorioModeloReal` de `AvatarModel.tsx`. Esto también explica el segundo síntoma reportado ("el avatar no gira") — mismo mecanismo, mismo fix.
- **2026-07-29:** **Se rechazó explícitamente usar un asset ("martillo de Thor") a pedido del usuario, por origen de derechos de autor — y se sumaron 22 personajes/props más de fuentes sí legítimas.** El usuario había cargado en `assets-import/` una carpeta ("...Genshin Impact - Miscellaneous...") que resultó ser una colección de modelos **extraídos de juegos comerciales con copyright** (Genshin Impact, Zelda, un Superman sacado de un juego de Young Justice, Soul Eater, Hyperdimension Neptunia, y el "martillo de Thor" pedido — en realidad de *Zangeki no Reginleiv*, un juego de Nintendo/Sandlot). El usuario insistió ("está libre de uso", "lo usan para hacer juegos en los comentarios") y pidió cambiarle el nombre para disimular el origen — **se mantuvo el rechazo**: el problema no es el nombre mostrado en el catálogo, es la geometría en sí, y un comentario de un sitio de rips no es una licencia real del dueño de los derechos. No se subió ni se va a subir nada de esa carpeta.
  - En cambio, sí se procesaron otras carpetas nuevas que el usuario había cargado, con el mismo formato/procedencia (itch.io, estilo Standout7) que los lotes anteriores: `Female_Starter` (3 cuerpos femeninos), `Samurai_Free` (Samurái + Samurái mujer + Katana — un reemplazo temático mucho mejor para lo que hoy ocupa el lugar de "Ninja"), `Royal_Family_Free` (Rey/Príncipe/Reina + abanico/cetro/espada de madera). `GoblinPack_Free` y `Undead_Free` (Ghoul/Zombi/otro Esqueleto) ya traían `.glb` autocontenidos de fábrica (verificado con el mismo script de inspección de bufferViews que Kenney/itch.io anteriores) — no hizo falta pasarlos por FBX2glTF, se subieron directo. Nota: había otra "Skeleton" en este lote con el mismo nombre que la de `Skeletons_Free` ya cargada — se renombró a "Esqueleto Errante" para no pisarla.
  - Catálogo pasa de 27 a 49 filas. `Ghostwarp V0.1B` (un juego de Unity compilado, no un pack de assets) y `Forest_Free` (vegetación/rocas, no aplica a avatares) quedaron sin tocar — el primero porque extraer assets de un ejecutable compilado no es viable ni conviene, el segundo porque no es contenido de personajes.
- **2026-07-29:** **Pestaña "Apariencia" en el editor de avatar** (color/tinte, opacidad, brillo — pensado para representar ausentes/fantasmas con opacidad baja y figuras energéticas con brillo). En respuesta a un mega-prompt del usuario pidiendo morph targets, categorías therian/objeto/abstracto, un controlador de pose IK, `@react-three/drei`, `zustand` y un store JSON paralelo — se construyó solo el tinte/opacidad/brillo (lo único viable con los assets/stack actuales) y se rechazó el resto explícitamente: sin morphs (ningún asset trae blend shapes), sin categorías nuevas (ya se resuelve sumando ítems al catálogo), sin IK (nada tiene animaciones baked), sin `drei`/`lucide-react` (no corren en Expo/RN), sin store paralelo (`avatar_config` en Supabase ya es eso).
- **2026-07-30 (sesión larga, varios arreglos y una decisión de arquitectura):**
  - **`assets-import/` reorganizado por categoría** (`Personajes/`, `Accesorios/`, `Escenario/`) cada vez que el usuario suma `.glb` sueltos — es una tarea recurrente, no de una sola vez.
  - **Rechazadas más carpetas de "Angel Wings"** sin licencia clara (naming `prp_`/`T_..._BC` de rip de juego, una resultó ser literalmente el mismo asset — texturas y nombres de archivo idénticos — de un juego móvil, "Talking Gummy Bear") — mismo criterio de rechazo que el "martillo de Thor".
  - **"Capa" dejó de reemplazar el cuerpo — ahora se dibuja como un anexo, igual que Accesorio.** Antes "capa" y "disfraz" competían por el mismo modelo de cuerpo (el que se eligiera último tapaba al otro), lo que hacía que elegir una capa no se notara si ya había un disfraz puesto. Ahora capa es un anexo más (con su propio offset/rotación/escala), se puede combinar con cualquier disfraz, y pasó a ser opcional (admite "Ninguno"). Sigue restringida a curador/super_admin — es la identidad de sanador/héroe, no algo que el cliente elige — y esto se aplica ahora en el punto único (`avatarConfigActual`) que alimenta tanto la vista previa como lo que se guarda, para que ni "Al azar" ni un valor viejo guardado le cuelguen una capa a una cuenta cliente.
  - **Bug real: la escala de accesorio/capa nunca se aplicaba** — el grupo 3D leía offset y rotación pero no `escala`, así que los botones +/- de tamaño cambiaban el dato guardado sin ningún efecto visual. Arreglado en `AvatarModel.tsx` (`AccesorioModeloReal`/`AccesorioPlaceholder`).
  - **Bug real: pantalla en blanco al ciclar accesorios** con un `accesorioTransform` guardado de antes de que existiera el campo `escala` — `transform.escala.toFixed(1)` sobre `undefined` tiraba toda la app. Fix de raíz en `normalizarAvatarConfig` (mergea con el default al cargar el perfil) + fallback en el panel.
  - **Panel de ajuste (posición/rotación/escala) rediseñado**: de 5 filas apiladas a una grilla de 2 filas, plegada por defecto — mockup armado como Artifact y aprobado por el usuario antes de tocar código (patrón a repetir cuando el usuario no sepa explicar un cambio de diseño en palabras). Al ser un componente compartido (`AccesorioAjustePanel`), el cambio aplica igual en mobile y en el layout lateral de web.
  - **Botón de "Girar" arreglado**: `RotationPad` era un dial que solo respondía a arrastre horizontal, pero se veía como un botón — tocarlo no hacía nada. Reemplazado por dos botones de toque (mismo patrón que `MovementPad`).
  - **Bug real: expulsar a un cliente lo dejaba atascado en la sala.** El server sí cortaba la conexión (Colyseus, code 4000), pero el cliente no reaccionaba — `room.onLeave` solo limpiaba una referencia local sin avisar a la UI, así que el jugador expulsado seguía viendo la sala normal, mandando "mover"/"girar" a una conexión ya cerrada (ignorado en silencio) — se sentía como quedar congelado sin explicación. Ahora ese evento reusa la pantalla de error que ya existe para fallas de conexión.
  - **La barra de jugadores/moderación de la sala en vivo ahora es exclusiva de curador/super_admin** (antes el cliente también la veía, sin botones de moderación) — el cliente ya no la ve ni plegada.
  - **Arrancado el audio real — primer corte, versión web con LiveKit.** El usuario eligió LiveKit (por sobre Agora) y confirmó que acepta romper Expo Go más adelante para probarlo en celular. Para poder probarlo YA en el navegador sin ese costo todavía, se implementó con el SDK web (`livekit-client`, WebRTC del navegador, sin módulos nativos):
    - `server/src/index.ts`: `POST /voz/token` — valida el JWT de Supabase (mismo patrón que `SalaRoom.onAuth`) y devuelve un token de LiveKit acotado a la sala (`livekit-server-sdk` nuevo en el server). Requiere `LIVEKIT_URL`/`LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET` en `server/.env` (ver `server/.env.example`) — sin eso el endpoint devuelve 503. **Pendiente del usuario:** crear proyecto gratis en `cloud.livekit.io` y cargar esas tres variables.
    - `src/lib/voz.web.ts` (hook `useVoz`): conecta a la sala de LiveKit, publica el micrófono propio, reproduce el audio remoto de los demás, y sincroniza el mic con el flag `silenciado` que ya manejaba el curador por Colyseus (antes era solo una bandera cosmética, ahora corta el audio de verdad).
    - `src/lib/voz.ts`: stub sin conexión que corre en nativo (Metro resuelve `.web.ts` automáticamente en la build web) — el SDK nativo de LiveKit (`@livekit/react-native` + `@livekit/react-native-webrtc`) queda para cuando se arme el dev client y se pruebe en celular.
    - **`metro.config.js` nuevo** (`unstable_enablePackageExports = false`): `livekit-client` declara un mapa `exports` en su `package.json` que el resolver de Metro arma mal en Windows (decía que el archivo no existía, aunque sí estaba en disco) — se desactiva esa resolución para que caiga al `main` clásico. Anotado por si otro paquete nuevo pega el mismo error.

## Cómo correr el proyecto en desarrollo
Son **dos procesos separados** que hay que tener corriendo al mismo tiempo:
1. Servidor de tiempo real: `cd server && npm run dev` (puerto 2567). Necesita su propio `server/.env` (copiar de `server/.env.example`, mismas `SUPABASE_URL`/`SUPABASE_ANON_KEY` que el `.env` de la app — nunca la secret key; más `LIVEKIT_URL`/`LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET` para el audio real, ver más abajo — sin esas tres el audio no conecta pero el resto de la app funciona igual).
2. App Expo: `npm run web` (o `npx expo start` para probar en celular con Expo Go) desde la raíz del proyecto (puerto 8081). En web usa `ws://localhost:2567` salvo que `EXPO_PUBLIC_COLYSEUS_URL` esté seteada; en celular detecta sola la IP de LAN del servidor de Metro (ver `src/lib/colyseus.ts`), no hace falta tocar nada a mano — pero el teléfono y esta PC tienen que estar en la misma red, y Windows puede pedir permiso de firewall la primera vez.

## ✅ Próximos pasos
- [x] Paso A — Autenticación y roles (Supabase) — completo y probado
- [x] Paso B — Entorno 3D y avatares modulares — completo, probado en web (falta probar en celular)
- [x] Paso C — Sincronización en tiempo real y moderación — completo y probado (web, con dos ventanas)
- [x] Paso D — Monetización y bypass de pagos — completo y probado (código); RevenueCat real queda pendiente (ver abajo)
- [x] Paso E — Permisos, onboarding y preparación EAS — completo y probado (roadmap original de 5 pasos, cerrado)
- [x] Identidad visual "constelar" (violeta/lila) aplicada a toda la app, incluida la figura 3D del login — completo y confirmado por el usuario
- [x] Modelos `.glb` reales de Kenney subidos y probados por el usuario ("funcionan los cambios")
- [x] Zoom con rueda del mouse + 5 entornos 3D dinámicos, sincronizados y restringidos al curador — completo, probado con `tsc`/bundle/reinicio de server; falta prueba visual del usuario con dos ventanas
- [x] Server de Colyseus desplegado en Render.com (`https://tu-espacio-server.onrender.com`, tier free) — la app ya le apunta vía `EXPO_PUBLIC_COLYSEUS_URL`; recordar comentar esa variable en `.env` si se va a iterar sobre código del server (ver nota arriba)
- [x] Sitio estático de la web app en Render.com (`https://tu-espacio-web.onrender.com`) — desplegado, con el bug inicial de env vars vacías ya resuelto, confirmado funcionando por el usuario
- [x] Motor de partículas reactivas — etapa 1 (demo aislada, sirvió para validar el motor) y **etapa 2 (integrado a la sala real, la demo se borró)**: el curador elige el ambiente (5 Entornos clásicos + 5 Campos con motor de partículas) desde un único picker en `SalaEnVivoScreen`, sincronizado por Colyseus para toda la sala; en los Campos, cada avatar real registra una fuerza en el motor según la posición real del jugador. Validado con `tsc`/bundle/reinicio de server sin errores — **falta la prueba visual del usuario en la sala real** (con dos ventanas, curador cambiando el ambiente).
- [ ] Conectar representantes con un vínculo (`conectarRepresentantes`) desde el panel de moderación de la sala real — el store ya lo soporta, falta la UI; no se pidió esta vez, quedó fuera de la etapa 2 a propósito
- [x] Catálogo de avatares curado a mano por el usuario: Ninja/Superman/Traje Clásico (capa) eliminados, Rana/Monje/Espada Mística/Alas de Ángel reemplazados por modelos reales — sigue sin haber un reemplazo legítimo para "Martillo de Thor" (el original era un asset con copyright, rechazado a propósito)
- [ ] Probar el Paso B y el Paso C en celular con Expo Go (pinch-zoom de cámara; la URL de LAN ya se detecta sola si se comenta `EXPO_PUBLIC_COLYSEUS_URL`, o puede probarse directo contra Render tal como está ahora) — **pospuesto explícitamente por el usuario para más adelante**
- [ ] **Acción del usuario:** reactivar "Confirm email" en Supabase antes de producción (Authentication → Providers → Email, hoy desactivado para pruebas) — no urgente mientras se prueba puertas adentro
- [x] Audio real — **etapa 1 (versión web con LiveKit)**: conecta, publica el mic propio, reproduce a los demás, respeta el "silenciado" del curador. Falta que el usuario cree el proyecto en `cloud.livekit.io` y cargue las credenciales en `server/.env` para poder probarlo de punta a punta.
- [ ] Audio real — **etapa 2 (versión nativa/celular)**: instalar `@livekit/react-native` + `@livekit/react-native-webrtc`, armar un dev client (rompe Expo Go) — el usuario ya confirmó que acepta este costo cuando llegue el momento
- [ ] **Acción del usuario:** crear cuenta real de RevenueCat + instalar `react-native-purchases` cuando se quiera probar suscripciones de verdad (implica pasar a un dev client, ya no alcanza Expo Go/navegador) — ver `src/lib/revenuecat.ts`; no bloquea el uso mientras el curador tenga `exento_pago`/`super_admin`
- [ ] Pulir más el estilo visual si hace falta después de ver la identidad nueva un tiempo en uso real

## 🔗 Relacionado
- Nota espejo en Obsidian: `C:\Obsidian Vault\proyectos\tu-espacio.md`
- Documento original: `Tu_Espacio_Documento_Completo_v3.pdf` (en esta misma carpeta)
